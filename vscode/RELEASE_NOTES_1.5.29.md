# Release Notes 1.5.29

## EN

### What’s New

- Added clearer Marketplace positioning for the full shipped feature set
- Highlighted `Goal Loop` for autonomous goal execution with judge-based completion checks
- Highlighted `Project Oracle RAG` for automatic workspace indexing and semantic retrieval
- Highlighted `design systems`, `code review`, `browser automation`, and `vision`
- Highlighted `cron tasks`, `Agent Marketplace`, `team sync`, and `MCP server` support

### Short Promo

Autonomous coding workspace for VS Code with chat, tools, memory, automation, and design workflows in one extension.

### Store Blurb

cvr.name.coder is an autonomous AI coding agent for VS Code with persistent memory, Goal Loop, Project Oracle RAG, design systems, code review, browser automation, vision, cron tasks, Agent Marketplace, team sync, MCP tools/server, and local or cloud LLM support.

## RU

### Что нового

- Обновлено Marketplace-описание, чтобы оно отражало весь уже внедрённый функционал
- Подсвечен `Goal Loop` для автономного выполнения целей с judge-проверкой результата
- Подсвечен `Project Oracle RAG` для авто-индексации проекта и семантического поиска
- Подсвечены `design systems`, `code review`, `browser automation` и `vision`
- Подсвечены `cron tasks`, `Agent Marketplace`, `team sync` и поддержка `MCP server`

### Короткий промо-текст

Автономное рабочее пространство для разработки в VS Code: чат, инструменты, память, автоматизация и дизайн-процессы в одном расширении.

### Короткое описание для витрины

cvr.name.coder — автономный AI coding agent для VS Code с постоянной памятью, Goal Loop, Project Oracle RAG, design systems, code review, browser automation, vision, cron tasks, Agent Marketplace, team sync, MCP tools/server и поддержкой локальных и облачных LLM.
